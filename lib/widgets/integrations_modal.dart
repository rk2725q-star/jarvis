import 'package:flutter/material.dart';

class IntegrationsModal extends StatelessWidget {
  const IntegrationsModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
